# Features

Contains all Retrofit for Gemini functionality.

Each feature owns everything it needs:

- components
- hooks
- styles
- utilities
- storage helpers

Features should be self-contained and interact with each other through shared abstractions rather than direct dependencies whenever possible.
