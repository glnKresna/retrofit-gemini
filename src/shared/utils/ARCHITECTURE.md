# Utilities

Small reusable helper functions.

Examples:

- debounce()
- throttle()
- clamp()
- formatTime()

Avoid placing large systems here.

If a utility grows into a module with its own responsibility, give it its own folder instead.
