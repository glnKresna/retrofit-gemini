# Shared React Hooks

Reusable React hooks shared across multiple features.

Examples:

- useStorage()
- useMutationObserver()
- useKeyboardShortcut()
- useTheme()

Hooks that are only used by one feature should stay inside that feature.
