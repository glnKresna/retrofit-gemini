# Shared

Reusable code shared across multiple parts of the project.

Nothing in this folder should implement a ChargeGPT feature.

Examples:

- reusable UI components
- DOM utilities
- storage wrappers
- ChatGPT helper functions
- custom hooks
- constants

If code is used by only one feature, it should probably stay inside that feature.
