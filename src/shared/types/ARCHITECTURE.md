# Shared Types

Contains TypeScript types shared throughout the project.

Types that are only used by one feature should stay inside that feature.

Only move a type here when it becomes shared by multiple parts of the application.
