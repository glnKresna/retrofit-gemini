# Extension Popup

The React application shown when the user clicks the extension icon.

The popup should remain lightweight.

Its purpose is to expose extension-level controls rather than duplicate functionality already available inside ChatGPT.
